API_KEY=dev-local-key
BRAVE_ENABLED=0
BRAVE_API_KEY=
EXA_API_KEY=
TAVILY_API_KEY=
DATABASE_URL=sqlite:///./data/intel.db
BLOB_ROOT=./data/blobs
# Agent Reach social side toolkit (default off) — WANd.INTEL.AGENT_REACH_SOCIAL.001
# INTEL_REACH_ENABLED=0
# YOUTUBE_API_KEY=
# Optional: Indonesian SOCKS/HTTP proxy for brittle *.go.id (BKPM etc.)
# PROXY_URL=socks5://127.0.0.1:1080
# INTEL_FETCH_L2=1
# Jina Reader fail-only after L1→L1.5→L2 (default off) — WANd.INTEL.JINA_FETCH_FALLBACK.001
# INTEL_FETCH_JINA_FALLBACK=0
# JINA_API_KEY=
# PDF async queue for pdf_too_large (default on) — WANd.INTEL.FETCH_PDF_QUEUE.001
# INTEL_PDF_QUEUE_ENABLED=1
# python -m jobs.cli_pdf_queue --limit 5
# python -m jobs.cli_pdf_queue --revert-queued
